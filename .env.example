# Copy to .env and fill in for the site this blueprint is targeting.

# Base URL the suite runs against. Falls back to http://localhost:3000 if unset.
PW_BASE_URL=https://example.com

# If the site needs authenticated tests, add credentials here and wire up a
# setup/*.setup.ts + storageState, following triadtest's pattern.
# USER_EMAIL=
# USER_PASSWORD=
